rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.6
  - 2.2.2
  - jruby-1.7.10
before_install:
  - gem install bundler
script: "bundle exec rake"
bundler_args: "--without repl documentation"
branches:
  only:
    - master
